name: Release

on:
  push:
    tags:
      - v*

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Helm Publisher
        uses: stefanprodan/helm-gh-pages@v1.5.0
        with:
          charts_dir: .
          token: ${{ github.token }}
